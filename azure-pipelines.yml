trigger:
  branches:
    include:
    - '*'

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: UsePythonVersion@0
  inputs:
    versionSpec: '3.8.x'
    addToPath: true
    architecture: 'x64'

- script: |
    pip install --upgrade pip
    pip install -r requirements.txt
    pip install -r requirements-dev.txt
  displayName: 'Install dependencies'

- script: python -m pytest --junitxml results.xml -s unit_tests/*.py regression_tests/*.py integ_tests/*.py
  displayName: 'Running unit tests'
  
- script: |
    flake8
    pylint unit_tests regression_tests integ_tests app  --disable=no-member --disable=too-many-locals
  displayName: 'Checking style'

- task: PublishTestResults@2
  inputs:
    testResultsFormat: 'JUnit'
    testResultsFiles: '**/results.xml'
    mergeTestResults: true
    failTaskOnFailedTests: true
