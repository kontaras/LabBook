# Ant

## Get all of the ant targets
`ant -p`

## Commenting out code with ifs (ant-contrib)
    <if>
     <istrue value="false"/>
     <then>
        <!-- Stuff to comment out -->
     </then>
    </if>
